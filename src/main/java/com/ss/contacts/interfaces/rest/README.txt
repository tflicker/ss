All classes related to the REST interface are located in this package. 

The REST classes only handle RESTful Request / Response interactions with the client.

All non-interface logic resides in the 'core' packages. 

The ServletLoader class registers all REST servlets with the application